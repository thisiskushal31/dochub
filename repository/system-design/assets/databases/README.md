# Database assets

Diagrams for the **databases** section are included **inline** in the markdown files as **Mermaid** diagrams (e.g. in [Database Design Overview](../../databases/1-database-design-overview.md), [Database Sharding](../../databases/4-database-sharding.md), [Storage Systems](../../databases/3-storage-systems.md)), so the repo stays self-contained without external image URLs.

You can add optional PNG/SVG exports of those diagrams here (e.g. `sharding-overview.png`) and reference them from the md files if you want standalone image files in addition to Mermaid.
